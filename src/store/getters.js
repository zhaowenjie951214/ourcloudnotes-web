const getters = {
  NowServerDate: state => state.window.NowServerDate,
  visible: state => state.window.visible,
  top: state => state.window.top,
  left: state => state.window.left,
}
export default getters